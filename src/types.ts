import { ShorthandPropertyAssignment } from "typescript";

export type User = {
  firstName?: string;
  lastName?: string;
  city?: string;
  email?: string;
  address?: string;
  postCode?: string;
};
